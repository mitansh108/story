# Public assets

Drop static files here. To replace the placeholder avatar with a real photo:

1. Save a square JPG (recommended 800x800) as `avatar.jpg` in this folder.
2. Update the `src` prop in [`components/Hero.tsx`](../components/Hero.tsx)
   from `/avatar.svg` to `/avatar.jpg`.
